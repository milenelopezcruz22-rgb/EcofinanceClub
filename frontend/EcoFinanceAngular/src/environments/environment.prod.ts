// Entorno de producción (ng build --configuration production).
// Reemplazar esta URL por la URL pública real que Railway asigna al
// servicio backend (Settings > Networking > Public Networking en Railway).
export const environment = {
  production: true,
  apiBaseUrl: 'https://tu-app-production.up.railway.app/api'
};
